# dsh-mail-digest 开发笔记（面向 agent / 维护者）

> **这不是给用户看的文档。** 用户看 [`README.md`](./README.md) —— 那里只讲
> "能解决什么问题、怎么装、怎么用"。
>
> 这里放**实现细节与踩坑记录**，用途是：下一个 agent 接手这个仓库时，
> 不用把同样的坑再踩一遍。安装使用类问题一律看 README。

---

## 一、为什么不用服务端主题搜索

实测于 163：`UID SEARCH ALL` 能正常列出全部邮件，而
`UID SEARCH SUBJECT "DSH-"`、`"DSH"`、`"[DSH-"` 各种**通配前缀**形态**一律返回空**。
一旦依赖它，用户的回信连候选集都进不来，后面客户端逻辑再对也没用
（等于在下游修、上游已筛空）。

所以收信流程是：`UID SEARCH ALL` 拿 UID 列表 → `UID FETCH (FLAGS BODY.PEEK[])`
取回候选邮件 → **本地**比对主题（压缩空白后子串匹配）→ 本地按 `\Seen` 判断未读。

## 二、候选范围怎么定（出过一个很隐蔽的 bug）

早期实现是「取最近 `limit*4` 个 UID，**从最旧往最新扫**，扫满 `limit` 封就停」。
问题是**用户每收到一封摘要回一次，旧回信就把名额占满**，他最新那封审批回信排在
候选之外 —— 现象是「回了邮件批准，等很久毫无反应」，最后只能去界面点拒绝。
修复分三层：

1. `limit` 现在**只限制返回条数**，绝不在候选阶段截断
   （`rankCandidateUids` 是纯函数，有回归测试锁住）；
2. 候选**从最新往回**排队 —— 回复是实时的，旧邮件沉底无所谓；
3. 额外用**已发出的完整线程标记**做服务端精确定位（自己生成的 token 是唯一随机串，
   不受 MIME 编码影响），命中的排最前，**邮箱再大也不会漏**；
   服务端匹配不到时自动退回 1、2 的宽松扫描。

成本由 `window`（宽松扫描看最近多少封，默认 `max(limit*4, 40)`）一个旋钮控制。

**收尾切片还错过一次方向**：曾写 `results.slice(-limit)`，误以为"池子旧的在前、
尾就是最新"。实际池子是**扫描顺序（新→旧）**，`slice(-30)` 取的是**最旧的 30 封**，
把刚到的回信干脆利落切掉（实测池里 33 封、limit 30，最新 3 封全丢）。
现在抽成纯函数 `pickNewestMails`：按 **UID 数值降序**取前 `limit` 封。

同处还有两个连带 bug：
- `results.push({ uid: key, ..., ...parsed })` —— `...parsed` 展开在 `uid` **之后**，
  而 parsed 里没有 uid 键，把每封邮件的 uid 覆盖成 `undefined`，
  导致 `pollOnce` 的 `consumedUids` 去重全失效（第一封之后每封都被当"已消费"）。
  **展开顺序必须是 `...parsed` 在最前**；
- 解析单封邮件抛错会**中断整个循环**（无 try/catch），后面所有邮件无声消失。
  必须逐封 try/catch。

## 三、界面与邮件怎么仲裁

原实现是纯 `Promise.race([邮件, 界面])`「先到算谁」。但**界面点一下是瞬间的、
邮件往返要几十秒**，所以邮件几乎必输。实测 2026-09-23 14:21：

```
14:21:02  插件发出审批邮件
14:21:19  用户回信正文 "1"（批准）
14:21:2x  界面返回 rejected → 工具当场失败
```

现在 `arbitrateApproval`（纯函数，有单元测试）的规则：

| 情况 | 结果 |
| --- | --- |
| 邮件先给结论 | 以邮件为准 |
| 界面批准 | 立刻生效，不必等（放行更宽松，无安全代价） |
| 界面拒绝 / 不可用 | **不立刻定论**，给邮件 45 秒宽限期 |
| 界面拒绝 + 窗口内无邮件 | 按界面结论（fail closed） |

配套：审批邮件发出后**立刻主动收一次信**，不要干等定时器
（定时器最长 15 秒，而实测用户 17 秒就回了）。

## 四、未读积压会把新回信挤出候选

收信分两轮：先只看未读，再全量兜底。原实现有一句
"未读那轮有返回就 break"（想省一次 IMAP 往返）—— 那是个**会累积**的缺陷：
未读堆起来后，"只看未读"那一轮的名额被旧邮件占满，用户新发的信反而进不了候选，
而全量兜底又因为 break 永远不执行。用户手动标已读之后恢复正常，所以极易被
误判成偶发故障。

修正：**两轮都跑** + **处理完的回信标为已读**（`markSeen`）。

标记判定刻意严格：**只有真的把结论交出去了才标**（审批已决 / 答案已采纳 /
回信已投回会话）。认不出、发件人不对、References 不匹配、解析不出内容、
投回失败 —— 一律**保持未读**，让用户能看见能重试。
开关：`reply.markProcessedAsRead = false`。

### 自查陷阱

诊断"未读有多少"时，别从 `BODY.PEEK[]` 的原文里正则找 `FLAGS` ——
那个响应**根本不含 FLAGS**，会得出完全错误的数字（据此报过"46 封全未读"，
实际只有 1 封）。要拿 flags 必须在 FETCH 里**显式请求 `FLAGS`**。

## 五、其它实测坑

| 坑 | 症状 | 处理 |
| --- | --- | --- |
| 163 要求 `ID` 命令 | 登录后 `SELECT` 被拒：`NO SELECT Unsafe Login` | 登录后先 `sendClientId` |
| QQ 给主题插空格 | `[DSH-Q:x]` → `[D SH-Q:x]`，token 中间也会断 | 压掉空白后再比对标记 |
| GBK/GB18030 正文 | `toString('utf8')` 会毁掉字节 | 保留 `rawBytes` Buffer，按 charset 用 `TextDecoder` |
| 服务端主题搜索不可靠 | 见第一节 | 取回候选后本地匹配 |

## 六、端到端验证记录（2026-09-23，真实邮箱）

```
16:13:4x  发起提权（需要写工作区外的文件）
16:13:5x  插件发出审批邮件 [DSH-A:1awydjcguhjo]
16:14:07  用户在手机上回信，正文「1」
16:14:20  提权被放行，工具执行成功
```

这一次**界面弹框同时也在**（用户就在电脑前），是邮件先到、邮件赢的。

### 同一个现象，五种不同病因

上面几个坑**全都表现为「用户回了邮件却毫无反应」**，且都不报错、日志干净：

| # | 病因 | 判据 |
| --- | --- | --- |
| 1 | 客户端把回信标 `\Seen`，而只搜未读 | 两轮搜索：未读 + 全量兜底 |
| 2 | 候选扫描扫满 `limit` 就 break、且从最旧往新扫 | `limit` 从 20 调到 60，命中数 0 → 1 |
| 3 | 仲裁用纯 `race`，界面瞬间取胜 | 日志里邮件批准与界面结论时间几乎相同 |
| 4 | 收尾 `slice(-limit)` 取的是池子末尾（最旧那批） | 池里 33 封、limit 30，最新 3 封全丢 |
| 5 | 未读积压挤出候选（且 break 跳过全量兜底） | 用户手动标已读后恢复 |

**排查纪律**：别推理，**把中间数据打出来看**。打印"候选池里有什么、推入了什么、
最终返回了什么"，三行日志就能把范围从五种缩到一种。验证分三层：
**取到没有 → 认对没有 → 采纳没有**，逐层确认再往下查。

这类"静默丢弃"的 bug 上层测试拦不住（替身绕过了真正出错的代码），
必须把排序/截断抽成纯函数单独测。

## 七、发布时带不带 skill

**DSH 没有"插件内置 skill"的机制。** 插件就是 bundle（`dsh.bundle.patch`），
skill 是另一套东西、装在 `~/.dsh/skills/`，给 **agent** 看的操作知识，两者发布上分开。

所以本仓库的约定：

- `README.md` —— 面向**用户**：能解决什么问题、怎么装、怎么用。
- 本文件 —— 面向**agent / 维护者**：实现细节与踩坑记录。
- 顶层的 `dsh-plugin-mgmt` 技能（不在本仓库）—— 安装/发布流程类的通用知识。
