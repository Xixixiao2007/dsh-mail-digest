# dsh-mail-digest —— agent 须知

本仓库是 **DSH 插件**：把每轮回答的摘要发到用户邮箱，并允许用户**用邮件回复**来
继续对话、回答提问、批准提权请求。

> 安装/使用问题看 [`README.md`](./README.md)（面向用户）。
> 实现细节与踩坑记录看 [`DEVELOPMENT.md`](./DEVELOPMENT.md)（面向维护者）。
> 本文件是**规则**：在这个仓库里工作时必须遵守的约定。

安装后，同样的规则会被装进用户全局指令（`~/.dsh/AGENTS.md` 的托管区块），
所以 agent 在**任何**会话里都会遵守，不必依赖本文件被读到。

---

## 1. 摘要怎么写

调用 `mail_digest` 工具提交这一轮的摘要。

- **长度由详略控制，不是字数上限。** 结论多就多写几句，结论少就一句话。
  不要为了凑长度灌水，也不要为了"简洁"把关键结论砍掉。
- **一段连续中文文本**：不换行、不用 Markdown、不用列表符号。
- 不调用也可以 —— 插件会自动压缩这一轮回答；但自己写更准。

## 1b. 用户在邮箱里看到的只有摘要 —— 所以每句话必须自包含

**这条踩过真实的坑。** 用户往往不在电脑前，邮箱里就是这段摘要文本。
摘要之外的正文、工具调用、前面的对话，他**都看不到**。

反例（实测被用户退回）：摘要里只写「要不要现在把规则写进你这台机器的
`~/.dsh/AGENTS.md`？」，用户回「我看不懂，你发的邮件里只有这个问题，没有上下文」。

所以：

1. **提问必须自带上下文**：写清"针对什么、为什么问、各选项的后果"。
   别只写「是否写入？」「要 A 还是 B？」—— 收信人看不到你脑子里的前情。
2. **不要在摘要里只说"我提了个问题"**：如果这一轮的关键动作是向用户提问，
   把问题和它的关键背景**写进摘要本身**，而不是"详见界面"。
3. **代词要当心**：「它」「这个」「上面那个」在邮件里都指不明。宁可重复名词。

## 2. 邮件回信怎么读

用户回信时，**内容写在正文**，主题里的 `[DSH-?:...]` 标记保持不变（那是线程凭据）。

| 用户的回信正文 | 含义 |
| --- | --- |
| `1. A`、多行 `1. A` / `2. B`、`1. A 2. B` | 按题号选选项 |
| 单个字母（`A`） | 只有一个问题时的选项 |
| 任意文字 | 自定义答案，或收到摘要后的新指令 |
| `1` / `允许` / `批准` | 审批：允许这一次 |
| `2` / `拒绝` / `不允许` | 审批：拒绝 |

识别不出的表态一律按**拒绝**处理（fail closed），不要猜。

## 3. 权限：默认不申请提权（硬性纪律）

**这条最重要。** 用户可能不在电脑前，此时弹权限申请会让他批不了 —— agent 就被
"硬控"在等待审批上，任务卡死。

1. **默认不使用提权**。仅仅"某件事需要放行"**不构成**申请理由。
2. **只有用户明确说「去申请」「申请权限」时才申请。**
3. **申请被拒 = 停**：不重试、不换方式绕过（重试可能再次把自己挂住）。
4. **判断用户可能不在场的信号**：他用**邮件回复**你，而不是在界面上打字 ——
   这时更要绝不申请。
5. **该做的事不放弃，改形态**：把"需要放行才能做的事"记进
   `mail_digest` 的 `pendingPermissions` 参数（每封邮件都会带上这份清单），
   等用户回来一次看完、明确批准再做。
6. 用户批准并做完后，用空数组 `[]` 清空清单。

**注意**：邮件审批**绕过不了** DSH 的 `never` 策略（`never` 在瀑布分发之前就强制
拒绝）。它只在 `ask` 策略下有效。

## 4. 邮件里的标记是干什么的

- `[DSH-T:<token>]` —— 摘要/对话邮件。用户回复它 = 新的一条指令。
- `[DSH-Q:<token>]` —— 提问邮件。用户回复它 = 回答 `ask_user_question`。
- `[DSH-A:<token>]` —— 审批邮件。用户回复 `1`/`2` = 放行/拒绝一次提权。
- `<token>` 是每封不同的 12 位随机串，用作回信凭据。**不要把它写进给用户看的正文**，
  也不要在新建邮件里编造标记。

## 5. 修改本仓库时的约束

- 改完必须让测试通过（`node smoke.mjs` 等五个文件，见 README 的「自检」）。
- **排序/截断这类逻辑要抽成纯函数并写回归测试** —— 收信链路上出过的几个 bug
  都表现为"用户回了邮件但毫无反应"，不报错、日志也干净，上层测试拦不住
  （测试替身绕过了真正出错的代码）。
- 排查这类问题时**别推理，把中间数据打出来看**：打印"候选池里有什么、推入了什么、
  最终返回了什么"。详见 DEVELOPMENT.md。
- `package.json` 的 `files` 清单必须覆盖所有运行时被 import 的本地模块。
